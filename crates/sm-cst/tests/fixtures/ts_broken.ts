// A deliberately unparseable file. `parse` must still return Ok, the tree must
// still account for every byte, and `has_errors()` must be true.

import { alpha from "./helpers";

export class Broken<T {
  private value: = ;

  method(a: number, : string) {
    if (a > {
      return
    }
  }
